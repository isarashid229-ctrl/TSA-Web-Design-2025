const dirRoot=document.getElementById('directory-root');const cardTpl=document.getElementById('resource-card');const filters=document.getElementById('filters');const resultsCount=document.getElementById('results-count');const resetBtn=document.getElementById('reset-filters');const activeFiltersEl=document.getElementById('active-filters');const azChips=document.getElementById('az-chips');let DATA=[];let typingTimer;(function buildAZ(){const frag=document.createDocumentFragment();const all=document.createElement('button');all.textContent='All';all.dataset.letter='';all.className='active';frag.appendChild(all);for(let i=65;i<=90;i++){const b=document.createElement('button');b.textContent=String.fromCharCode(i);b.dataset.letter=b.textContent;frag.appendChild(b);}azChips.appendChild(frag);azChips.addEventListener('click',e=>{const btn=e.target.closest('button');if(!btn)return;azChips.querySelectorAll('button').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const val=btn.dataset.letter||'';const hidden=document.createElement('input');hidden.type='hidden';hidden.name='initial';hidden.value=val;const prev=filters.querySelector('input[name="initial"]');if(prev)prev.remove();filters.appendChild(hidden);render();});})();(async function init(){try{const res=await fetch('data/resources.json?ts='+Date.now());if(!res.ok)throw new Error('HTTP '+res.status);const json=await res.json();if(!Array.isArray(json))throw new Error('resources.json must be an array');DATA=json;}catch(e){DATA=[];}render();})();function getFilters(){const fd=new FormData(filters);return{q:(fd.get('q')||'').toLowerCase().trim(),category:fd.get('category')||'',city:fd.get('city')||'',cost:fd.get('cost')||'',accessibility:fd.get('accessibility')||'',initial:(fd.get('initial')||'').toUpperCase(),sort:fd.get('sort')||'name-asc'};}function showActivePills(f){const pills=[];if(f.q)pills.push(`<span class="pill">Search: ${f.q}</span>`);if(f.category)pills.push(`<span class="pill">Category: ${f.category}</span>`);if(f.city)pills.push(`<span class="pill">Region: ${f.city}</span>`);if(f.cost)pills.push(`<span class="pill">Cost: ${f.cost}</span>`);if(f.accessibility)pills.push(`<span class="pill">Access: ${f.accessibility}</span>`);if(f.initial)pills.push(`<span class="pill">A–Z: ${f.initial}</span>`);activeFiltersEl.innerHTML=pills.join(' ');}function applyFilters(items,f){let out=items.filter(r=>{const name=(r.name||'');const desc=(r.description||'');const tags=(r.tags||[]).join(' ');const rCity=(r.city||'');const k=f.q?(name.toLowerCase().includes(f.q)||desc.toLowerCase().includes(f.q)||tags.toLowerCase().includes(f.q)):true;const c=f.category?r.category===f.category:true;const city=f.city?rCity===f.city:true;const cost=f.cost?r.cost===f.cost:true;const a=f.accessibility?(r.accessibility||[]).includes(f.accessibility):true;const letter=f.initial?(name[0]||'').toUpperCase()===f.initial:true;return k&&c&&city&&cost&&a&&letter;});switch(f.sort){case 'name-desc':out.sort((a,b)=>b.name.localeCompare(a.name));break;case 'city-asc':out.sort((a,b)=>(a.city||'').localeCompare(b.city||''));break;case 'city-desc':out.sort((a,b)=>(b.city||'').localeCompare(a.city||''));break;case 'updated-desc':out.sort((a,b)=>new Date(b.updated)-new Date(a.updated));break;default:out.sort((a,b)=>a.name.localeCompare(b.name));}return out;}function render(){const f=getFilters();showActivePills(f);const items=applyFilters(DATA,f);dirRoot.innerHTML='';resultsCount.textContent=`${items.length} result${items.length!==1?'s':''}`;if(items.length===0){dirRoot.innerHTML=`<article class="card"><h3>No results</h3><p class="card-desc">Try clearing filters or a different keyword.</p></article>`;return;}for(const r of items){const node=cardTpl.content.cloneNode(true);const name=r.name||'Untitled';const title=node.querySelector('.card-title');if(r.url&&r.url!=='#'){title.innerHTML=`<a class="card-link" href="${r.url}" target="_blank" rel="noopener noreferrer nofollow">${name}</a>`;}else{title.textContent=name;}const bits=[];if(r.city)bits.push(r.city);if(r.category)bits.push(r.category);if(r.cost)bits.push(r.cost);if(r.updated){const d=new Date(r.updated);if(!isNaN(d))bits.push('updated '+d.toLocaleDateString());}node.querySelector('.card-meta').textContent=bits.join(' • ');node.querySelector('.card-desc').textContent=r.description||'';node.querySelector('.card-tags').textContent=(r.tags||[]).map(t=>`#${t}`).join(' ');const linkBtn=node.querySelector('a.btn');if(r.url&&r.url!=='#'){linkBtn.href=r.url;linkBtn.setAttribute('aria-label',`Visit ${name}`);}else{linkBtn.remove();}dirRoot.appendChild(node);}}filters?.addEventListener('input',ev=>{clearTimeout(typingTimer);if(ev.target.tagName==='SELECT'){render();return;}typingTimer=setTimeout(render,160);});resetBtn?.addEventListener('click',()=>{filters.reset();azChips.querySelectorAll('button').forEach(b=>b.classList.remove('active'));azChips.querySelector('button')?.classList.add('active');const prev=filters.querySelector('input[name="initial"]');if(prev)prev.remove();render();});document.addEventListener('filters-change',render);dirRoot?.addEventListener('click',ev=>{const card=ev.target.closest('.card');if(!card)return;const link=card.querySelector('.card-link');const interactive=ev.target.closest('a,button,input,select,textarea,label');if(link&&!interactive){link.click();}});dirRoot?.addEventListener('keydown',ev=>{if(ev.key==='Enter'){const card=ev.target.closest('.card');if(!card)return;const link=card.querySelector('.card-link');link?.click();}});